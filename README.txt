This is a cross platform process wrapper.

It is used for starting a process, monitoring it, and restarting it if
it terminates unsucessfully.

The build script is included in the source file. Just run the source file as
a shell script to produce the program:

  ./wrapper.cpp
